<Window x:Class="TestPlugin.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestPlugin.Views"
             mc:Ignorable="d" Height="400" Width="300" Title="Тестовый плагин"
             WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid>
        <StackPanel>
            <TextBlock>Категории элементов в модели:</TextBlock>
            <ListBox x:Name="Categories" Height="150" ScrollViewer.VerticalScrollBarVisibility="Visible">></ListBox>
            <TextBlock>Список параметров в категории:</TextBlock>
            <ListBox x:Name="SelectedCategoryParams" Height="150" ScrollViewer.VerticalScrollBarVisibility="Visible"></ListBox>
            <Button x:Name="EditCategoryParam" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="80">Изменить</Button>
        </StackPanel>
        <DockPanel>
            
        </DockPanel>
        <Button x:Name="Close" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="80">Закрыть</Button>
        
    </Grid>
</Window>
